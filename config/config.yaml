server:
  host:
    name: "localhost"
    port: 8080
database:
  path: "databases/tinyis.db"
super_organization:
  name : "root"
  admin:
    username: "admin"
    password: "admin"
crypto:
  jwa:
    path: "resources/crypto/jwa"
  server:
    key: "resources/crypto/server/server-key.pem"
    cert: "resources/crypto/server/server-cert.pem"
